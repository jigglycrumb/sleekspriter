todo
----------------------------------------------------------------
- composite canvas (offscreen)
- statusbar: farbe des aktuellen pixels anzeigen (braucht composite canvas)
- preview-window (braucht composite canvas)
- eyedropper tool (braucht composite canvas)

- toolbox, layerbox: scrollbars wenn bildschirm zu klein
- brush tool: paletten, auto-palette

- hotkeys für tools
- support für frames
- workspace in localStorage speichern

- layer per drag & drop sortieren
- layer mergen
- hand tool
- hotkeys für copy/paste, layer merge etc



später (in node)
----------------------------------------------------------------
- speichern/laden in dateien
- menüs
  pixler
    about pixler
    search for updates
    preferences
    quit
  file
    new
    open
      recent
    save
    export
  layer
    merge with layer below
    merge with layer above